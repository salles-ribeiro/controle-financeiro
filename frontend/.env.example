# URL base da API do backend. Em desenvolvimento o proxy do Vite já
# encaminha /api para http://localhost:3001, então deixe como está.
# Em produção, se o frontend for hospedado separado do backend,
# aponte para a URL pública da API (ex.: https://api.seudominio.com/api)
VITE_URL_BASE_DA_API=/api
